[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "sdd-cli"
version = "3.2.1"
description = "Spec-Driven Development scaffolding for AI coding agents"
authors = [{name = "Mário Henrique", email = "developer.mario.santos@example.com"}]
readme = "README.md"
requires-python = ">=3.10"
dependencies = []          # stdlib only, on purpose

[project.scripts]
sdd = "sdd_cli.cli:main"

[tool.hatch.build.targets.wheel]
packages = ["src/sdd_cli"]
