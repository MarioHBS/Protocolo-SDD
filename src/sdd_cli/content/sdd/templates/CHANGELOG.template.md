<!-- STRUCTURAL CHANGE LOG — produced by sdd-init, appended by sdd-close.
     Records every change to locked decisions (§2/§3), every mid-flight
     excursion back to DECIDING, and every renumbering — with date, reason
     and affected stages. The constitution §6 keeps only a dated-band index
     pointing here.
     The structural labels (this comment, the H1, the intro, the section
     heading) stay in English. The entry text ("what changed and why") is
     written in the project language (Settings > Language). -->

# Structural change log

> Append-only narrative of structural changes: locked-decision edits, DECIDING
> excursions, renumberings. Each entry has a date, a reason and the affected
> stages. The constitution's §6 is a dated-band index pointing here; this file
> is the long-form history.

## Entries

<!-- One line per change, newest at the top. Format:
     - [YYYY-MM-DD] — [what changed and why, project language] — affected stages: [NNN, ...]
     Keep the date format, the em dashes and the "affected stages:" label as-is;
     write the entry text in the project language. -->

- [YYYY-MM-DD] — [first entry placeholder, replaced at first sdd-close] — affected stages: []
